---
import KattIBoks from "@img/KattIBoks";
import LanguageRedirecter from "@language-redirecter/LanguageRedirecter";
import { text } from "@language/text";
import Layout from "@layouts/Layout.astro";
import { BodyLong, Heading } from "@navikt/ds-react";
import { baseUrl } from "@src/urls";
import styles from "../_error.module.css";

const language = Astro.currentLocale;
---

<Layout title="Utkast" breadcrumbs={[]}>
  <div class={styles.container}>
    <Heading size={"xlarge"}>{text.errorHeading["nn"]}</Heading>
    <KattIBoks alt={text.errorKitten["nn"]} />
    <BodyLong>
      <strong>{text.errorText["nn"]}</strong>
    </BodyLong>
    <BodyLong>
      {text.errorHelp["nn"]} <a href={baseUrl}>{text.minSide["nn"]}</a>
    </BodyLong>
  </div>
</Layout>
<LanguageRedirecter client:only="react" language={language}/>